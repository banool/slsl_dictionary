name: Deploy landing site to Cloudflare Pages

# The deploy itself is a reusable workflow in the dictionarylib repo
# (app-pages-deploy.yaml); this is the thin SLSL-specific caller.

on:
  push:
    branches: [main]
    paths: ['site/**', '.github/workflows/pages.yaml']
  workflow_dispatch:

# Single deployment at a time.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    uses: banool/dictionarylib/.github/workflows/app-pages-deploy.yaml@main
    with:
      project_name: slsl-landing
      production_branch: main
    secrets:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
