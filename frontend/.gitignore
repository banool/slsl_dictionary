# My stuff
# Local dictionarylib path override for dev (matches auslan_dictionary). For
# CI/release the app resolves dictionarylib from the git ref in pubspec.yaml.
pubspec_overrides.yaml
# Build caches that shouldn't be tracked.
.wrangler/
# Downloaded videos + extracted first-frame posters used only when capturing
# Android screenshots (take_screenshots.py); a regenerable cache, not an artifact.
screenshots/.posters/
ios/**/xcshareddata/swiftpm/
ios/publish_do_not_check_in.sh
ios/fastlane/report.xml
ios/Runner.ipa
ios/Runner.app.*
.vscode
ios/*.cer
ios/fastlane/screenshots
ios/fastlane/Preview.html
ios/build
ios/Flutter/ephemeral

# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
/build/

# Web related

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json
